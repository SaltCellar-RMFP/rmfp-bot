import type { MessageReaction, PartialMessageReaction, PartialUser, User } from 'discord.js';

export const updateRMFPScore = async (
	reaction: MessageReaction | PartialMessageReaction,
	user: PartialUser | User,
) => {};
